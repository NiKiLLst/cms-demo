services:
  pocketbase:
    build: .
    volumes:
      # Only pb_data is stateful (SQLite DB + uploaded files). pb_migrations and
      # pb_hooks are code and live in the image so updates ship via rebuild.
      - pocketbase_data:/pb/pb_data
    expose:
      - "8090"

volumes:
  pocketbase_data:
