# Public URL of the Directus admin API. The frontend talks to this server-side
# (so the value never leaves the Coolify network in production).
CMS_URL=http://directus-admin.nerdass.org

# Public-facing name used in the "powered by" badge.
CMS_NAME=Directus
